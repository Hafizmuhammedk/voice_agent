[project]
name = "voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hafizmuhammedk", email = "hafizmuhammed1019@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "livekit-agents[google]~=1.7.0",
    "livekit-plugins-ai-coustics>=0.3.0",
    "livekit-plugins-google~=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
voice-agent = "voice_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
